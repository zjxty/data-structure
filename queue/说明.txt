数据结构：“队列”的模拟实现

队列也是一种极其常见的数据结构，其特点为“后进先出”，生活中像排队买挂号，先排队的能先挂到号
队列的插入删除 是一种非常高效的操作，时间复杂度为O(1)；插入数据只能在队列尾处插入，删除数据只能在队列头处删除

本文使用链表 并用c语言 来模拟实现队列，具体实现的功能有：
* 队列的初始化和销毁
* 队列的插入删除
* 队列头尾元素的获取
* 队列中元素的打印
  ...
  
需要注意的是：
* 测试文件test.c中，队列结构体是在内存的栈区创建的，不能通过free来释放队列结构体
* 因为只能从队列尾向队列头依次遍历，打印队列中元素则采用递归的方式